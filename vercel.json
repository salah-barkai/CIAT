{
  "version": 2,
  "installCommand": "npm install && npm install --prefix client && npm install --prefix server && npm run build --prefix client",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["server/ciat-knowledge.json", "client/dist/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
